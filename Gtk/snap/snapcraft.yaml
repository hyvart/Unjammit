name: unjammit # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '0.0.0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: JCF track and score player # 79 char long summary
description: |
  Play your JCF media controlling track volume, progress, and switching
  between instrument scores.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
